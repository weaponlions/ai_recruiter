name: CI — api-gateway

on:
  push:
    branches: [main, develop]
    paths:
      - 'apps/api-gateway/**'
      - 'packages/**'
      - '.github/workflows/ci-gateway.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'apps/api-gateway/**'
      - 'packages/**'

env:
  NODE_VERSION: '20'

jobs:
  lint-typecheck:
    name: Lint & Type-check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4


      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile --network-concurrency 2

      - name: Lint
        run: yarn turbo run lint --filter=@hr-ai/api-gateway...

      - name: Type-check
        run: yarn turbo run type-check --filter=@hr-ai/api-gateway...

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4


      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile --network-concurrency 2

      - name: Run tests
        run: yarn turbo run test --filter=@hr-ai/api-gateway

  docker-build:
    name: Docker Build Validation
    runs-on: ubuntu-latest
    needs: [lint-typecheck]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image (validation only)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/api-gateway/Dockerfile
          push: false
          tags: hr-ai/api-gateway:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max
