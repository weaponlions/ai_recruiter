name: CI — audit-service

on:
  push:
    branches: [main, develop]
    paths:
      - 'apps/audit-service/**'
      - 'packages/**'
      - '.github/workflows/ci-audit.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'apps/audit-service/**'
      - 'packages/**'

env:
  NODE_VERSION: '20'
  PNPM_VERSION: '9'

jobs:
  lint-typecheck:
    name: Lint & Type-check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm turbo run lint --filter=@hr-ai/audit-service...
      - run: pnpm turbo run type-check --filter=@hr-ai/audit-service...

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: audit_db_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/audit_db_test
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - name: Generate Prisma client
        run: pnpm --filter=@hr-ai/audit-service exec prisma generate
      - run: pnpm turbo run test --filter=@hr-ai/audit-service

  docker-build:
    name: Docker Build Validation
    runs-on: ubuntu-latest
    needs: [lint-typecheck]
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/audit-service/Dockerfile
          push: false
          tags: hr-ai/audit-service:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max
